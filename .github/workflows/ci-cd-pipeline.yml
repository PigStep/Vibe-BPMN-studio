# TODO
# - Add type checking (mypy) to catch bugs early
# - Add security scanning (safety check) for vulnerable dependencies
# - Consolidate PR workflows (ruff_linter + pytest + docker_hadolint) into single ci-pr.yml
# - Consider: /health endpoint for container health checks

name: CI/CD - Build, Test & Deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

# TODO: add tests

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ vars.DOCKERHUB_USERNAME }}/${{ vars.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-render:
    runs-on: ubuntu-latest
    needs: build
    environment: demo-production

    steps:
      - name: Debug Info
        run: |
          echo "Event Name: ${{ github.event_name }}"
          echo "Actor: ${{ github.actor }}"
          echo "Previous job: ${{ needs.build.result }}"

      - name: Trigger Render Deployment
        run: |
          curl -f -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
